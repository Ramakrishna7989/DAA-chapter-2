#include <stdio.h>

int main() {
    // An empty list
    int emptyList[] = {};

    // A list with one element
    int oneElementList[] = {42};

    // A list with all identical elements
    int identicalList[] = {5, 5, 5, 5, 5};

    // A list with negative numbers
    int negativeList[] = {-1, -2, -3, -4, -5};

    return 0;
}
